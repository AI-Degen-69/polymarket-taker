# --- secrets ---
.env
.env.*
!.env.example
# Deploy reference: placeholders only, no real secrets. Must be committed or
# the Railway setup has nothing to copy from.
!deploy/.env.deploy.example

# --- runtime artifacts ---
*.pid
trades.db
trades.db-*
# Archived run databases — local experiment history, not source.
archive/
logs/

# --- python ---
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# --- node / vite ---
ui/node_modules/
ui/dist/
ui/.vite/

# --- raw research dumps (keep .md reports + .py scripts) ---
research/bonereader_raw/
research/*.log
research/*.json

# --- macOS ---
.DS_Store
bot.mode


# stray CLI artifacts
.railway-config-pull-*/
.railway/
